const FileFilterType = {
    IMAGE: "image",
    VIDEO: "video",
    DOCUMENT: "doc",
    AUDIO: "audio"
}

const UserRoles = {
    ADMIN: "admin",
    CUSTOMER: "customer",
    SELLER: "seller"
}

const StatusType = {
    ACTIVE: "active",
    INACTIVE:"inactive"
}

module.exports = {
    FileFilterType,
    UserRoles,
    StatusType
}